# 三欄式 CRM 介面設計
# 左側客戶列表 | 中間訊息歷史 | 右側客戶資訊

**設計日期**: 2025-11-05
**UI 框架**: Element Plus + TailwindCSS

---

## 整體布局

```
┌─────────────────────────────────────────────────────────────────────┐
│  [🏠 LINE CRM]  儀表板 | 統計 | 設定                     👤 客服名稱  │
├──────────┬──────────────────────────┬───────────────────────────────┤
│          │                          │                               │
│  客戶列表 │    訊息歷史（聊天室）      │      客戶 CRM 資訊            │
│  (左側)   │       (中間)             │        (右側)                │
│          │                          │                               │
│  300px   │    flex-grow (佔滿)       │      350px                    │
│          │                          │                               │
└──────────┴──────────────────────────┴───────────────────────────────┘
```

---

## 詳細設計

### 左側：客戶列表 (300px)

```
┌─────────────────────────────────────┐
│  🔍 [________搜尋客戶________]      │
│                                     │
│  篩選: [全部▾] [未讀▾] [標籤▾]     │
│                                     │
├─────────────────────────────────────┤
│  📊 統計                            │
│  • 總客戶: 67                       │
│  • 未讀: 3                          │
│  • 活躍: 45                         │
├─────────────────────────────────────┤
│  客戶列表（可滾動）                  │
│  ┌─────────────────────────────┐   │
│  │ 🟢 👤 王小明          [3] 未讀│   │
│  │    最後訊息: 我想訂購商品...   │   │
│  │    🏷️ VIP客戶 | 10:30        │   │
│  ├─────────────────────────────┤   │
│  │ ⚪ 👤 李美玲                  │   │
│  │    最後訊息: 請問有現貨嗎...   │   │
│  │    🏷️ 新客戶 | 昨天          │   │
│  ├─────────────────────────────┤   │
│  │ 🟢 👤 張大明          [1] 未讀│   │
│  │    最後訊息: 收到了，謝謝     │   │
│  │    🏷️ 回購客戶 | 11:20       │   │
│  ├─────────────────────────────┤   │
│  │ ... (更多客戶)               │   │
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

#### 功能說明

1. **搜尋框**
   - 即時搜尋客戶名稱、LINE ID、群組名稱
   - 支援模糊搜尋

2. **篩選器**
   - 全部客戶
   - 未讀訊息
   - 依標籤篩選
   - 活躍狀態

3. **統計資訊**
   - 快速概覽
   - 數字可點擊（快速篩選）

4. **客戶卡片**
   - 頭像（從 LINE Profile）
   - 客戶名稱（優先顯示真實姓名，其次群組名稱，最後 LINE 名稱）
   - 未讀數量徽章
   - 最後訊息預覽（截斷）
   - 標籤（最多顯示 2 個）
   - 時間（智能顯示：今天顯示時間，昨天/更早顯示日期）
   - 線上狀態（綠色=今天有訊息，灰色=無）

5. **排序**
   - 預設：最後訊息時間（新的在上）
   - 可選：未讀優先、客戶名稱

---

### 中間：訊息歷史（聊天室）(flex-grow)

```
┌───────────────────────────────────────────────────────────┐
│  👤 王小明  🏷️ VIP客戶               [🔍] [⚙️] [更多▾]   │
│  LINE ID: U1234567890abcdef                               │
├───────────────────────────────────────────────────────────┤
│  [今日]                                                   │
│                                                           │
│  ┌────────────────────────────────┐                      │
│  │ 👤 你好，我想訂購商品           │  ◀ 客戶訊息（左側） │
│  │    10:30                        │                      │
│  └────────────────────────────────┘                      │
│                                                           │
│                      ┌────────────────────────────────┐  │
│  客服訊息（右側） ▶  │ 好的，請問要哪一款？          │  │
│                      │ 👤 林小美（客服）10:31        │  │
│                      └────────────────────────────────┘  │
│                                                           │
│  ┌────────────────────────────────┐                      │
│  │ 👤 紅色的那款                   │                      │
│  │    10:32                        │                      │
│  └────────────────────────────────┘                      │
│                                                           │
│  ┌────────────────────────────────┐                      │
│  │ 🖼️ [圖片預覽]                   │                      │
│  │    product.jpg (245 KB)         │                      │
│  │    [⬇️ 下載] [👁️ 檢視]          │                      │
│  │    10:33                        │                      │
│  └────────────────────────────────┘                      │
│                                                           │
│  ... (可滾動)                                             │
│                                                           │
├───────────────────────────────────────────────────────────┤
│  發送訊息區域                                             │
│  ┌─────────────────────────────────────────────────────┐ │
│  │ [____________輸入訊息..._________________________] │ │
│  │                                                     │ │
│  │                                                     │ │
│  └─────────────────────────────────────────────────────┘ │
│                                                           │
│  [💬 快捷回覆▾] [📎 上傳檔案] [😊 表情]      [發送訊息]  │
└───────────────────────────────────────────────────────────┘
```

#### 功能說明

1. **頂部資訊欄**
   - 客戶頭像和名稱
   - 客戶標籤
   - 操作按鈕：
     - 🔍 搜尋訊息
     - ⚙️ 設定（通知、靜音等）
     - 更多（匯出對話、清空訊息等）

2. **訊息時間軸**
   - 日期分隔線（今日、昨天、日期）
   - 訊息氣泡：
     - 客戶訊息：左側，灰色背景
     - 客服訊息：右側，藍色背景
   - 顯示發送者（客服訊息顯示客服名稱）
   - 時間戳
   - 訊息狀態（已發送、失敗）

3. **訊息類型**
   - 文字訊息
   - 圖片（縮圖 + 預覽）
   - 檔案（圖示 + 檔名 + 大小 + 下載）
   - 音訊（播放器）
   - 視訊（播放器）

4. **捲動行為**
   - 載入時自動捲到最底部
   - 新訊息到達時自動捲動（如果已經在底部）
   - 向上滾動載入更多歷史訊息（無限滾動）

5. **發送區域**
   - 多行輸入框（可自動擴展高度）
   - 快捷回覆下拉選單
   - 檔案上傳按鈕
   - 表情符號選擇器
   - 發送按鈕（Ctrl+Enter 快捷鍵）

6. **快捷回覆**
   - 常用訊息模板
   - 可自訂
   - 一鍵插入

---

### 右側：客戶 CRM 資訊 (350px)

```
┌─────────────────────────────────────────┐
│  客戶資訊                    [✏️ 編輯]   │
├─────────────────────────────────────────┤
│  👤 基本資料                             │
│  ┌─────────────────────────────────┐   │
│  │ 真實姓名: 王曉明                 │   │
│  │ LINE 名稱: 王小明                │   │
│  │ 群組名稱: 王小明-訂購紀錄        │   │
│  │ LINE ID: U1234567...             │   │
│  │ 語言: 繁體中文                   │   │
│  │ 加入時間: 2025-10-01             │   │
│  │ 最後活躍: 2 分鐘前               │   │
│  └─────────────────────────────────┘   │
│                                         │
│  🏷️ 標籤                     [+ 新增]   │
│  ┌─────────────────────────────────┐   │
│  │ [VIP客戶 ×] [已購買 ×] [活躍 ×] │   │
│  └─────────────────────────────────┘   │
│                                         │
│  📊 統計資訊                             │
│  ┌─────────────────────────────────┐   │
│  │ 訊息總數: 48                     │   │
│  │ ├─ 客戶發送: 35                  │   │
│  │ └─ 客服回覆: 13                  │   │
│  │ 平均回覆時間: 5 分鐘             │   │
│  │ 首次訊息: 25 天前                │   │
│  └─────────────────────────────────┘   │
│                                         │
│  📝 備註                     [+ 新增]   │
│  ┌─────────────────────────────────┐   │
│  │ • 偏好紅色系商品                 │   │
│  │   林小美 | 2025-11-03            │   │
│  │                                  │   │
│  │ • 訂購記錄：已購買 3 次           │   │
│  │   張大華 | 2025-10-15            │   │
│  └─────────────────────────────────┘   │
│                                         │
│  📎 附件 (3)                 [查看全部]  │
│  ┌─────────────────────────────────┐   │
│  │ 📄 訂單-20251101.pdf             │   │
│  │ 🖼️ product.jpg                   │   │
│  │ 📊 price-list.xlsx               │   │
│  └─────────────────────────────────┘   │
│                                         │
│  🔗 相關連結                             │
│  ┌─────────────────────────────────┐   │
│  │ [📧 發送 Email]                  │   │
│  │ [📱 撥打電話]                    │   │
│  │ [📊 查看訂單]                    │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

#### 功能說明

1. **基本資料**
   - 顯示所有用戶資訊
   - 可編輯真實姓名
   - 顯示 LINE Profile 資料
   - 最後活躍時間（智能顯示）

2. **標籤管理**
   - 顯示所有標籤
   - 可新增/移除標籤
   - 標籤有顏色區分
   - 點擊標籤可篩選相同標籤的客戶

3. **統計資訊**
   - 訊息數量統計
   - 回覆時間統計
   - 互動頻率
   - 可視覺化（小型圖表）

4. **備註系統**
   - 時間軸式顯示
   - 記錄重要資訊
   - 顯示建立者和時間
   - 可編輯/刪除

5. **附件列表**
   - 快速存取該客戶發送的所有檔案
   - 分類顯示（圖片、文件、其他）
   - 點擊下載

6. **相關連結**
   - 快速操作按鈕
   - 可擴展（整合其他系統）

---

## 響應式設計

### 桌面版 (> 1200px)
```
[客戶列表 300px] [訊息歷史 flex] [CRM 資訊 350px]
```

### 平板版 (768px - 1200px)
```
[客戶列表 250px] [訊息歷史 flex] [CRM 資訊 摺疊]
```
- CRM 資訊變成浮動側邊欄
- 點擊「客戶資訊」按鈕展開

### 手機版 (< 768px)
```
頁面 1: [客戶列表 全寬]
頁面 2: [訊息歷史 全寬] (點擊客戶後進入)
頁面 3: [CRM 資訊 全寬] (點擊頂部客戶名稱展開)
```

---

## 實作結構

### 元件樹

```
App.vue
└── MainLayout.vue
    ├── AppHeader.vue
    ├── CustomerList.vue (左側)
    │   ├── CustomerSearch.vue
    │   ├── CustomerFilter.vue
    │   ├── CustomerStats.vue
    │   └── CustomerItem.vue (多個)
    ├── MessageThread.vue (中間)
    │   ├── ThreadHeader.vue
    │   ├── MessageList.vue
    │   │   ├── DateDivider.vue
    │   │   └── MessageItem.vue (多個)
    │   │       ├── TextMessage.vue
    │   │       ├── ImageMessage.vue
    │   │       ├── FileMessage.vue
    │   │       └── ...
    │   └── MessageSender.vue
    │       ├── QuickReplyDropdown.vue
    │       ├── FileUpload.vue
    │       └── EmojiPicker.vue
    └── CustomerCRM.vue (右側)
        ├── BasicInfo.vue
        ├── TagManager.vue
        ├── CustomerStats.vue
        ├── NotesList.vue
        │   └── NoteItem.vue (多個)
        ├── AttachmentList.vue
        └── QuickActions.vue
```

### 頁面檔案

```
src/
├── views/
│   └── Dashboard.vue (主頁面，包含三欄布局)
├── components/
│   ├── layout/
│   │   ├── MainLayout.vue
│   │   └── AppHeader.vue
│   ├── customer/
│   │   ├── CustomerList.vue
│   │   ├── CustomerItem.vue
│   │   ├── CustomerSearch.vue
│   │   ├── CustomerFilter.vue
│   │   └── CustomerStats.vue
│   ├── message/
│   │   ├── MessageThread.vue
│   │   ├── ThreadHeader.vue
│   │   ├── MessageList.vue
│   │   ├── MessageItem.vue
│   │   ├── DateDivider.vue
│   │   ├── TextMessage.vue
│   │   ├── ImageMessage.vue
│   │   ├── FileMessage.vue
│   │   └── MessageSender.vue
│   └── crm/
│       ├── CustomerCRM.vue
│       ├── BasicInfo.vue
│       ├── TagManager.vue
│       ├── CustomerStats.vue
│       ├── NotesList.vue
│       └── AttachmentList.vue
└── stores/
    ├── customers.js
    ├── messages.js
    └── crm.js
```

---

## Element Plus 元件使用

### 左側 - 客戶列表
- `el-input` - 搜尋框
- `el-select` - 篩選下拉
- `el-badge` - 未讀數量徽章
- `el-avatar` - 客戶頭像
- `el-tag` - 標籤
- `el-scrollbar` - 客戶列表滾動

### 中間 - 訊息歷史
- `el-card` - 訊息氣泡
- `el-image` - 圖片預覽
- `el-button` - 操作按鈕
- `el-dropdown` - 快捷回覆
- `el-upload` - 檔案上傳
- `el-input` (textarea) - 訊息輸入
- `el-divider` - 日期分隔線

### 右側 - CRM 資訊
- `el-descriptions` - 基本資料
- `el-tag` - 標籤
- `el-timeline` - 備註時間軸
- `el-statistic` - 統計數字
- `el-button-group` - 操作按鈕組

---

## 程式碼範例

### 主頁面結構

```vue
<!-- src/views/Dashboard.vue -->
<template>
  <div class="dashboard-layout">
    <!-- 頂部導航 -->
    <AppHeader />

    <!-- 三欄布局 -->
    <div class="main-content">
      <!-- 左側：客戶列表 -->
      <aside class="customer-sidebar">
        <CustomerList
          v-model:selected="selectedCustomerId"
          @select="handleCustomerSelect"
        />
      </aside>

      <!-- 中間：訊息歷史 -->
      <main class="message-main">
        <MessageThread
          v-if="selectedCustomerId"
          :customer-id="selectedCustomerId"
        />
        <el-empty
          v-else
          description="請選擇一個客戶查看訊息"
        />
      </main>

      <!-- 右側：CRM 資訊 -->
      <aside class="crm-sidebar">
        <CustomerCRM
          v-if="selectedCustomerId"
          :customer-id="selectedCustomerId"
        />
      </aside>
    </div>
  </div>
</template>

<script setup>
import { ref } from 'vue';
import AppHeader from '@/components/layout/AppHeader.vue';
import CustomerList from '@/components/customer/CustomerList.vue';
import MessageThread from '@/components/message/MessageThread.vue';
import CustomerCRM from '@/components/crm/CustomerCRM.vue';

const selectedCustomerId = ref(null);

function handleCustomerSelect(customerId) {
  selectedCustomerId.value = customerId;
}
</script>

<style scoped>
.dashboard-layout {
  display: flex;
  flex-direction: column;
  height: 100vh;
}

.main-content {
  display: flex;
  flex: 1;
  overflow: hidden;
}

.customer-sidebar {
  width: 300px;
  border-right: 1px solid var(--el-border-color);
  overflow-y: auto;
}

.message-main {
  flex: 1;
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

.crm-sidebar {
  width: 350px;
  border-left: 1px solid var(--el-border-color);
  overflow-y: auto;
}

/* 響應式 */
@media (max-width: 1200px) {
  .crm-sidebar {
    position: fixed;
    right: -350px;
    transition: right 0.3s;
    background: white;
    z-index: 100;
    height: 100%;
  }

  .crm-sidebar.open {
    right: 0;
  }
}

@media (max-width: 768px) {
  .main-content {
    flex-direction: column;
  }

  .customer-sidebar,
  .message-main,
  .crm-sidebar {
    width: 100%;
  }
}
</style>
```

### 客戶列表元件

```vue
<!-- src/components/customer/CustomerList.vue -->
<template>
  <div class="customer-list">
    <!-- 搜尋框 -->
    <el-input
      v-model="searchQuery"
      placeholder="搜尋客戶..."
      prefix-icon="Search"
      clearable
    />

    <!-- 篩選器 -->
    <div class="filters">
      <el-select v-model="filterType" placeholder="篩選">
        <el-option label="全部" value="all" />
        <el-option label="未讀" value="unread" />
        <el-option label="活躍" value="active" />
      </el-select>

      <el-select v-model="filterTag" placeholder="標籤">
        <el-option label="全部" value="" />
        <el-option
          v-for="tag in tags"
          :key="tag.id"
          :label="tag.name"
          :value="tag.id"
        />
      </el-select>
    </div>

    <!-- 統計 -->
    <div class="stats">
      <el-statistic title="總客戶" :value="totalCustomers" />
      <el-statistic title="未讀" :value="unreadCount" />
    </div>

    <!-- 客戶列表 -->
    <el-scrollbar class="customer-scroll">
      <CustomerItem
        v-for="customer in filteredCustomers"
        :key="customer.id"
        :customer="customer"
        :selected="selectedCustomerId === customer.id"
        @click="handleSelect(customer.id)"
      />
    </el-scrollbar>
  </div>
</template>

<script setup>
import { ref, computed } from 'vue';
import { useCustomersStore } from '@/stores/customers';
import CustomerItem from './CustomerItem.vue';

const customersStore = useCustomersStore();

const searchQuery = ref('');
const filterType = ref('all');
const filterTag = ref('');
const selectedCustomerId = ref(null);

const filteredCustomers = computed(() => {
  let customers = customersStore.customers;

  // 搜尋
  if (searchQuery.value) {
    const query = searchQuery.value.toLowerCase();
    customers = customers.filter(c =>
      c.display_name?.toLowerCase().includes(query) ||
      c.customer_name?.toLowerCase().includes(query) ||
      c.group_display_name?.toLowerCase().includes(query)
    );
  }

  // 篩選類型
  if (filterType.value === 'unread') {
    customers = customers.filter(c => c.unread_count > 0);
  } else if (filterType.value === 'active') {
    customers = customers.filter(c => c.is_active);
  }

  // 篩選標籤
  if (filterTag.value) {
    customers = customers.filter(c =>
      c.tags?.some(t => t.id === filterTag.value)
    );
  }

  return customers;
});

const totalCustomers = computed(() => customersStore.customers.length);
const unreadCount = computed(() =>
  customersStore.customers.filter(c => c.unread_count > 0).length
);

function handleSelect(customerId) {
  selectedCustomerId.value = customerId;
  emit('select', customerId);
}

const emit = defineEmits(['select']);
</script>
```

### 訊息元件

```vue
<!-- src/components/message/MessageItem.vue -->
<template>
  <div :class="['message-item', direction]">
    <div class="message-bubble">
      <!-- 文字訊息 -->
      <div v-if="message.message_type === 'text'" class="text-content">
        {{ message.text_content }}
      </div>

      <!-- 圖片訊息 -->
      <el-image
        v-else-if="message.message_type === 'image'"
        :src="message.file_path"
        :preview-src-list="[message.file_path]"
        fit="cover"
        style="max-width: 300px"
      />

      <!-- 檔案訊息 -->
      <div v-else-if="message.message_type === 'file'" class="file-message">
        <el-icon><Document /></el-icon>
        <span>{{ message.file_name }}</span>
        <span class="file-size">{{ formatFileSize(message.file_size) }}</span>
        <el-button size="small" @click="downloadFile">下載</el-button>
      </div>

      <!-- 底部資訊 -->
      <div class="message-meta">
        <span v-if="message.sent_by" class="sender">{{ message.sent_by }}</span>
        <span class="time">{{ formatTime(message.timestamp) }}</span>
        <el-icon v-if="message.send_status === 'failed'" class="error">
          <WarningFilled />
        </el-icon>
      </div>
    </div>
  </div>
</template>

<script setup>
import { computed } from 'vue';
import { Document, WarningFilled } from '@element-plus/icons-vue';

const props = defineProps({
  message: Object
});

const direction = computed(() =>
  props.message.direction === 'outbound' ? 'outbound' : 'inbound'
);

function formatTime(timestamp) {
  // 格式化時間邏輯
  const date = new Date(timestamp);
  return date.toLocaleTimeString('zh-TW', { hour: '2-digit', minute: '2-digit' });
}

function formatFileSize(bytes) {
  if (bytes < 1024) return bytes + ' B';
  if (bytes < 1024 * 1024) return (bytes / 1024).toFixed(1) + ' KB';
  return (bytes / (1024 * 1024)).toFixed(1) + ' MB';
}

function downloadFile() {
  // 下載檔案邏輯
  window.open(`/api/files/${props.message.file_id}`, '_blank');
}
</script>

<style scoped>
.message-item {
  margin-bottom: 16px;
  display: flex;
}

.message-item.inbound {
  justify-content: flex-start;
}

.message-item.outbound {
  justify-content: flex-end;
}

.message-bubble {
  max-width: 70%;
  padding: 12px 16px;
  border-radius: 12px;
  background-color: var(--el-fill-color-light);
}

.message-item.outbound .message-bubble {
  background-color: var(--el-color-primary);
  color: white;
}

.message-meta {
  margin-top: 4px;
  font-size: 12px;
  opacity: 0.8;
  display: flex;
  align-items: center;
  gap: 8px;
}
</style>
```

---

## 總結

這個三欄式布局：

✅ **左側客戶列表**
- 搜尋和篩選功能
- 即時顯示未讀數量
- 客戶卡片快速預覽

✅ **中間訊息歷史**
- 類似 LINE 的聊天介面
- 支援雙向訊息顯示
- 多種訊息類型
- 發送訊息功能

✅ **右側 CRM 資訊**
- 客戶完整資訊
- 標籤管理
- 統計資訊
- 備註系統

這是一個現代化、專業的 CRM 介面設計，適合客服人員日常使用。
